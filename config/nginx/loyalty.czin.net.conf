upstream puma_loyalty {
  server unix:/var/www/loyalty/shared/tmp/sockets/puma.sock fail_timeout=0;
}

# Redirect HTTP → HTTPS (apex + all shop subdomains)
server {
  listen 80;
  server_name loyalty.czin.net *.loyalty.czin.net;
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl http2;
  # Apex (super-admin, merchant, /w/:slug) + wildcard shop subdomains (white-label PWA)
  server_name loyalty.czin.net *.loyalty.czin.net;

  # SSL — managed by Certbot.
  #   Apex-only cert works for loyalty.czin.net; for valid TLS on shop
  #   subdomains install a wildcard cert (*.loyalty.czin.net via DNS-01) and
  #   point the two lines below at it.
  ssl_certificate     /etc/letsencrypt/live/loyalty.czin.net/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/loyalty.czin.net/privkey.pem;
  include             /etc/letsencrypt/options-ssl-nginx.conf;
  ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

  root /var/www/loyalty/current/public;

  add_header X-Frame-Options        SAMEORIGIN;
  add_header X-Content-Type-Options nosniff;
  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

  client_max_body_size 20M;

  gzip on;
  gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml application/manifest+json;
  gzip_min_length 1000;

  location ^~ /assets/ {
    gzip_static on;
    expires 1y;
    add_header Cache-Control public;
    break;
  }

  location ^~ /rails/active_storage/ {
    proxy_pass         http://puma_loyalty;
    proxy_set_header   Host $host;
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    proxy_read_timeout 120s;
  }

  # Sidekiq Web UI (protect behind app auth if mounted)
  location @puma {
    proxy_pass         http://puma_loyalty;
    proxy_set_header   Host $host;
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    proxy_redirect     off;
    proxy_read_timeout 120s;
  }

  try_files $uri @puma;
}
