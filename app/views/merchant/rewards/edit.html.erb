<% content_for :title, t("merchant.rewards.edit_title") %>
<% content_for :title_en, "EDIT REWARD" %>
<%= turbo_frame_tag "reward_form", data: { modal_target: "frame" } do %>
  <div class="l-modal-card">
    <div class="l-modal-head">
      <h3 class="l-display" style="font-size:20px; margin:0;"><%= t("merchant.rewards.edit_title") %></h3>
      <button type="button" class="l-modal-x" data-action="modal#close" aria-label="<%= t("merchant.rewards.cancel") %>">✕</button>
    </div>
    <%= render "form", reward: @reward, url: merchant_reward_path(@reward), method: :patch, submit_label: t("merchant.rewards.save") %>
  </div>
<% end %>
