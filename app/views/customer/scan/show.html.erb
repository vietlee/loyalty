<% content_for :title, t("customer.scan.title") %>
<% content_for :tab, "" %>

<div class="l-screen" data-controller="qrnav" data-qrnav-resolve-url-value="<%= member_scan_resolve_path %>"
     style="display:flex; flex-direction:column;">
  <header class="l-appbar" style="padding-left:0;">
    <%= link_to "←", member_root_path, style: "font-size:22px; text-decoration:none; color:var(--ink);" %>
    <div class="nm l-display" style="font-size:22px;"><%= t("customer.scan.title") %></div>
  </header>

  <div style="position:relative; aspect-ratio:1; border-radius:22px; overflow:hidden; background:#111; display:grid; place-items:center; margin-top:6px;">
    <video data-qrnav-target="video" playsinline muted style="width:100%; height:100%; object-fit:cover;"></video>
    <div data-qrnav-target="overlay" style="position:absolute; inset:0; color:#fff;">
      <div style="position:absolute; top:50%; left:50%; transform:translate(-50%,-50%); width:calc(100% - 48px); max-width:260px; display:flex; flex-direction:column; align-items:center; gap:10px; text-align:center;">
        <%= ui_icon(:scan, size: 42) %>
        <p style="font-size:13px; opacity:.85; line-height:1.4; margin:0;"><%= t("customer.scan.prompt") %></p>
        <button type="button" class="l-btn l-btn-primary" data-action="qrnav#start"><%= t("customer.scan.start_camera") %></button>
      </div>
    </div>
    <div style="position:absolute; inset:16%; border:3px solid rgba(255,255,255,.75); border-radius:20px; pointer-events:none;"></div>
  </div>
  <p data-qrnav-target="status" style="text-align:center; color:var(--ink-2); font-size:12px; margin-top:8px;"></p>

  <div style="display:flex; align-items:center; gap:10px; margin:16px 0; color:var(--ink-2); font-size:13px;">
    <span style="flex:1; height:1px; background:var(--line);"></span> <%= t("customer.scan.or_enter") %> <span style="flex:1; height:1px; background:var(--line);"></span>
  </div>
  <%= form_with url: member_scan_resolve_path, method: :get do %>
    <div style="display:flex; gap:8px;">
      <input name="code" class="l-field" placeholder="<%= t("customer.scan.enter_placeholder") %>" autocomplete="off">
      <%= submit_tag t("customer.scan.submit"), class: "l-btn l-btn-primary", style: "white-space:nowrap;" %>
    </div>
  <% end %>
</div>
